workflows:
  android-build:
    name: Android APK Build
    environment:
      java: 17
    scripts:
      - name: Build APK
        script: |
          gradle assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
